/**
 * CivicAuth React Native Wrapper - Core Logic Only
 * CivicAuth React Native 包装器 - 仅核心逻辑导出
 *
 * 只导出核心认证逻辑，不包含任何 UI 组件
 * Only exports core authentication logic, no UI components
 */
export { default as CivicAuth } from './CivicAuthModule';
export { loginWithCivic } from './CivicAuthModule'; 